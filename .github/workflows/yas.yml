name: demo

on:
  #push:
    branches: [ main ]
  #workflow_dispatch:
    env:
      env_prod = prod
    var: 
      var_prod = testing_prod
jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: run a job
        run: echo successful {{$env.env_prod}} & {{var.var_prod}}

    
  
    
